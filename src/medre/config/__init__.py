"""MEDRE configuration — models, paths, loaders, env, and adapter config dataclasses.

This package does not expose a stable public API.
Import directly from concrete modules:
  from medre.config.model import RuntimeConfig
  from medre.config.paths import MedrePaths
  from medre.config.loader import load_config
  from medre.config.env import MedreEnvConfig
  from medre.config.routes import RouteConfig
"""
