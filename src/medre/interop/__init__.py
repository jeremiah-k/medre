"""Cross-adapter protocol constants.

Constants that define wire-format contracts shared between transport adapters
live in sub-modules of this package.  They are intentionally kept outside any
single adapter tree so that boundary tests do not need special exemptions.
"""
