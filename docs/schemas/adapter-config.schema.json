{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://medre.dev/schemas/adapter-config.schema.json",
  "title": "AdapterConfig",
  "description": "Adapter configuration shapes for all supported transports. Each adapter has its own frozen config dataclass.",
  "oneOf": [
    {
      "title": "MatrixConfig",
      "description": "Immutable configuration for the Matrix adapter.",
      "type": "object",
      "properties": {
        "adapter_id": {
          "type": "string",
          "pattern": "^(?!(?:[Cc][Oo][Nn]|[Pp][Rr][Nn]|[Aa][Uu][Xx]|[Nn][Uu][Ll]|[Cc][Oo][Mm][1-9]|[Ll][Pp][Tt][1-9])(?:[.]|$))(?!.*[.]$)[A-Za-z0-9][A-Za-z0-9._-]*$",
          "maxLength": 255,
          "description": "Unique configured adapter identifier: starts with a letter or digit; letters, digits, dots, hyphens, and underscores only; max 255 characters; Windows-reserved device names and trailing periods are rejected. Uniqueness across transports and environment-token collisions are enforced by the loader/runtime, not by this schema."
        },
        "homeserver": {
          "type": "string",
          "description": "Matrix homeserver URL (must start with 'http://' or 'https://')."
        },
        "user_id": {
          "type": "string",
          "description": "Fully-qualified Matrix user ID (must start with '@')."
        },
        "device_id": {
          "type": ["string", "null"],
          "default": null,
          "description": "Device ID. Internal — discovered via whoami() on login."
        },
        "access_token": {
          "type": "string",
          "default": "",
          "description": "Access token for authentication."
        },
        "room_allowlist": {
          "type": ["array", "null"],
          "items": { "type": "string" },
          "default": null,
          "description": "Optional set of room IDs to accept messages from. Null means all rooms."
        },
        "metadata_embedding_mode": {
          "type": "string",
          "default": "safe",
          "description": "How metadata is embedded in messages.",
          "enum": ["off", "minimal", "safe", "full"]
        },
        "store_path": {
          "type": ["string", "null"],
          "default": null,
          "description": "E2EE store path. Internal — derived by runtime."
        },
        "sync_timeout_ms": {
          "type": "integer",
          "default": 30000,
          "minimum": 0,
          "description": "Timeout in milliseconds for long-polling sync requests."
        },
        "sync_stale_timeout_seconds": {
          "type": "number",
          "minimum": 0,
          "default": 300.0,
          "description": "Maximum seconds without durable sync progress before MEDRE recycles the current Matrix sync loop; 0 disables active stale-sync recovery. Typed validation requires a positive value to exceed sync_timeout_ms/1000 + 15 seconds."
        },
        "megolm_key_request_rate_limit_per_minute": {
          "type": "integer",
          "minimum": 1,
          "default": 30,
          "description": "Maximum outbound missing-room-key to-device request attempts per rolling minute."
        },
        "megolm_key_request_max_inflight": {
          "type": "integer",
          "minimum": 1,
          "default": 4,
          "description": "Maximum concurrent detached missing-room-key recovery tasks."
        },
        "encryption_mode": {
          "type": "string",
          "default": "plaintext",
          "enum": ["plaintext", "e2ee_required", "e2ee_optional"],
          "description": "Encryption policy."
        },
        "require_encrypted_rooms": {
          "type": "boolean",
          "default": false,
          "description": "Whether to only operate in encrypted rooms."
        },
        "auto_join_rooms": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Canonical Matrix room IDs ('!localpart:server') to auto-join on startup."
        },
        "origin_label": {
          "type": "string",
          "default": "",
          "description": "Operator-facing human-readable source label for relay prefixes."
        },
        "relay_prefix": {
          "type": "string",
          "default": "",
          "description": "Target-local prefix template for Matrix outbound body text. Uses {placeholder} syntax resolved by the shared prefix formatter."
        }
      },
      "required": ["adapter_id", "homeserver", "user_id"],
      "additionalProperties": false
    },
    {
      "title": "MeshtasticConfig",
      "description": "Immutable configuration for the Meshtastic adapter.",
      "type": "object",
      "properties": {
        "adapter_id": {
          "type": "string",
          "pattern": "^(?!(?:[Cc][Oo][Nn]|[Pp][Rr][Nn]|[Aa][Uu][Xx]|[Nn][Uu][Ll]|[Cc][Oo][Mm][1-9]|[Ll][Pp][Tt][1-9])(?:[.]|$))(?!.*[.]$)[A-Za-z0-9][A-Za-z0-9._-]*$",
          "maxLength": 255,
          "description": "Unique configured adapter identifier: starts with a letter or digit; letters, digits, dots, hyphens, and underscores only; max 255 characters; Windows-reserved device names and trailing periods are rejected. Uniqueness across transports and environment-token collisions are enforced by the loader/runtime, not by this schema."
        },
        "connection_type": {
          "type": "string",
          "enum": ["fake", "tcp", "serial", "ble"],
          "default": "fake",
          "description": "Connection mode."
        },
        "host": {
          "type": ["string", "null"],
          "default": null,
          "description": "Hostname or IP for TCP connections."
        },
        "port": {
          "type": ["integer", "null"],
          "default": null,
          "description": "Port number for TCP connections."
        },
        "serial_port": {
          "type": ["string", "null"],
          "default": null,
          "description": "Serial device path for serial connections."
        },
        "ble_address": {
          "type": ["string", "null"],
          "default": null,
          "description": "BLE MAC address for BLE connections."
        },
        "origin_label": {
          "type": "string",
          "default": "",
          "description": "Operator-facing human-readable source label for relay prefixes."
        },
        "default_channel": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Default radio channel index for outbound messages."
        },
        "channel_mapping": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "default": {},
          "description": "Mapping of channel index to human-readable channel name (display labels only)."
        },
        "message_delay_seconds": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "description": "Minimum delay between outbound messages (pacing)."
        },
        "startup_backlog_suppress_seconds": {
          "type": "number",
          "default": 5.0,
          "minimum": 0,
          "description": "Seconds after start to suppress stale backlog packets."
        },
        "sync_timeout_ms": {
          "type": "integer",
          "default": 30000,
          "description": "Timeout in milliseconds for sync operations."
        },
        "radio_relay_prefix": {
          "type": "string",
          "default": "{sender_short}: ",
          "description": "Template prepended to messages relayed from Matrix to Meshtastic radio. Default preserves MMRelay wire parity for relayed text; set \"\" explicitly for prefix-free output."
        },
        "mmrelay_compatibility": {
          "type": "boolean",
          "default": false,
          "description": "Embed mmrelay-compatible Meshtastic metadata in Matrix event content."
        },
        "max_text_bytes": {
          "type": "integer",
          "default": 227,
          "minimum": 0,
          "description": "Maximum UTF-8 byte budget for final radio text after rendering."
        },
        "queue_send_max_attempts": {
          "type": "integer",
          "default": 3,
          "minimum": 1,
          "description": "Maximum send attempts per queued item (first attempt + retries)."
        },
        "queue_max_size": {
          "type": "integer",
          "default": 1024,
          "minimum": 1,
          "description": "Hard adapter-local outbound queue capacity."
        },
        "queue_warning_threshold_pct": {
          "type": "number",
          "default": 75.0,
          "exclusiveMinimum": 0,
          "exclusiveMaximum": 100,
          "description": "Queue utilization percentage that enters advisory warning pressure."
        },
        "queue_critical_threshold_pct": {
          "type": "number",
          "default": 90.0,
          "exclusiveMinimum": 0,
          "exclusiveMaximum": 100,
          "description": "Queue utilization percentage that enters critical pressure and degrades adapter health. Typed validation also requires this to exceed the warning threshold."
        },
        "reconnect_backoff_initial_seconds": {
          "type": "number",
          "default": 1.0,
          "exclusiveMinimum": 0,
          "description": "Initial MEDRE session-level reconnect delay after a started adapter loses transport."
        },
        "reconnect_backoff_max_seconds": {
          "type": "number",
          "default": 30.0,
          "exclusiveMinimum": 0,
          "description": "Maximum MEDRE session-level reconnect delay. Typed validation requires this to be at least the initial delay."
        },
        "tcp_liveness_interval_seconds": {
          "type": "number",
          "default": 60.0,
          "minimum": 0,
          "description": "Interval between active TCP round-trip liveness probes; 0 disables active probing."
        },
        "tcp_liveness_timeout_seconds": {
          "type": "number",
          "default": 30.0,
          "exclusiveMinimum": 0,
          "description": "Maximum seconds to wait for the TCP liveness request metadata response before session-level recovery."
        },
        "outbound_mode": {
          "type": "string",
          "enum": ["enabled", "listen_only"],
          "default": "enabled",
          "description": "Controls whether outbound radio sends are enabled."
        },
        "encrypted_action": {
          "type": "string",
          "enum": ["drop", "deferred"],
          "default": "drop",
          "description": "Classification policy for encrypted packets whose payload cannot be inspected. 'drop' discards them; 'deferred' queues them for deferred processing."
        },
        "chat_portnums": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": [],
          "description": "Frozenset of Meshtastic port-num names treated as chat traffic for classification. Empty means no portnums are explicitly classified as chat at the config level."
        },
        "disabled_portnums": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": [],
          "description": "Frozenset of Meshtastic port-num names whose packets are dropped before classification. Empty means no portnums are disabled."
        },
        "detection_sensor_relay": {
          "type": "boolean",
          "default": false,
          "description": "Whether DETECTION_SENSOR app packets are relayed across the bridge."
        }
      },
      "required": ["adapter_id"],
      "additionalProperties": false
    },
    {
      "title": "MeshCoreConfig",
      "description": "Immutable configuration for the MeshCore adapter.",
      "type": "object",
      "properties": {
        "adapter_id": {
          "type": "string",
          "pattern": "^(?!(?:[Cc][Oo][Nn]|[Pp][Rr][Nn]|[Aa][Uu][Xx]|[Nn][Uu][Ll]|[Cc][Oo][Mm][1-9]|[Ll][Pp][Tt][1-9])(?:[.]|$))(?!.*[.]$)[A-Za-z0-9][A-Za-z0-9._-]*$",
          "maxLength": 255,
          "description": "Unique configured adapter identifier: starts with a letter or digit; letters, digits, dots, hyphens, and underscores only; max 255 characters; Windows-reserved device names and trailing periods are rejected. Uniqueness across transports and environment-token collisions are enforced by the loader/runtime, not by this schema."
        },
        "connection_type": {
          "type": "string",
          "enum": ["fake", "tcp", "serial", "ble"],
          "default": "fake",
          "description": "Connection mode."
        },
        "host": {
          "type": ["string", "null"],
          "default": null,
          "description": "Hostname or IP for TCP connections."
        },
        "port": {
          "type": ["integer", "null"],
          "default": null,
          "description": "Port number for TCP connections."
        },
        "serial_port": {
          "type": ["string", "null"],
          "default": null,
          "description": "Serial device path for serial connections."
        },
        "serial_baudrate": {
          "type": "integer",
          "default": 115200,
          "description": "Baud rate for serial connections."
        },
        "ble_address": {
          "type": ["string", "null"],
          "default": null,
          "description": "BLE MAC address for BLE connections."
        },
        "origin_label": {
          "type": "string",
          "default": "",
          "description": "Operator-facing human-readable source label for relay prefixes."
        },
        "default_channel": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Default radio channel index for outbound messages."
        },
        "message_delay_seconds": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "description": "Minimum delay between outbound messages (pacing)."
        },
        "identity": {
          "type": ["string", "null"],
          "default": null,
          "description": "Optional MeshCore node identity string."
        },
        "pubkey": {
          "type": ["string", "null"],
          "pattern": "^[0-9a-fA-F]+$",
          "default": null,
          "description": "Optional MeshCore public key as a hex string."
        },
        "node_config": {
          "type": "object",
          "additionalProperties": true,
          "default": {},
          "description": "Opaque dict for future node-specific settings. Must not contain secret keys: validate-time enforcement in MeshCoreConfig rejects keys whose lowercased name matches {'private_key', 'secret', 'password'} (case-insensitive). Schema-level additionalProperties remains open by design — the runtime validator is the source of truth for the secret-key blocklist."
        },
        "max_text_bytes": {
          "type": "integer",
          "default": 512,
          "minimum": 0,
          "description": "Maximum UTF-8 byte budget for final radio text after rendering."
        },
        "ble_pin": {
          "type": ["string", "null"],
          "default": null,
          "description": "Optional BLE pairing PIN. Sensitive; forwarded only to the SDK create_ble call; never exposed in diagnostics or logs."
        },
        "meshcore_relay_prefix": {
          "type": "string",
          "default": "",
          "description": "Optional relay prefix template prepended to outbound text before byte-budget truncation. Empty string means no prefix. Uses {placeholder} syntax resolved by the shared prefix formatter."
        }
      },
      "required": ["adapter_id"],
      "additionalProperties": false
    },
    {
      "title": "LxmfConfig",
      "description": "Immutable configuration for the LXMF adapter.",
      "type": "object",
      "properties": {
        "adapter_id": {
          "type": "string",
          "pattern": "^(?!(?:[Cc][Oo][Nn]|[Pp][Rr][Nn]|[Aa][Uu][Xx]|[Nn][Uu][Ll]|[Cc][Oo][Mm][1-9]|[Ll][Pp][Tt][1-9])(?:[.]|$))(?!.*[.]$)[A-Za-z0-9][A-Za-z0-9._-]*$",
          "maxLength": 255,
          "description": "Unique configured adapter identifier: starts with a letter or digit; letters, digits, dots, hyphens, and underscores only; max 255 characters; Windows-reserved device names and trailing periods are rejected. Uniqueness across transports and environment-token collisions are enforced by the loader/runtime, not by this schema."
        },
        "connection_type": {
          "type": "string",
          "enum": ["fake", "reticulum"],
          "default": "fake",
          "description": "Connection mode. 'fake' for testing, 'reticulum' for real LXMF."
        },
        "display_name": {
          "type": "string",
          "default": "",
          "description": "Optional display name for LXMF announces."
        },
        "stamp_cost": {
          "type": "integer",
          "default": 8,
          "minimum": 0,
          "maximum": 254,
          "description": "LXMF inbound stamp cost. 0 disables stamps; valid positive range is 1..254."
        },
        "default_delivery_method": {
          "type": "string",
          "enum": ["direct", "opportunistic", "propagated", "paper"],
          "default": "direct",
          "description": "Default LXMF delivery method."
        },
        "outbound_propagation_node": {
          "type": ["string", "null"],
          "pattern": "^[0-9A-Fa-f]{32}$",
          "default": null,
          "description": "Optional 16-byte outbound LXMF propagation-node destination hash as 32 hexadecimal characters."
        },
        "origin_label": {
          "type": "string",
          "default": "",
          "description": "Operator-facing human-readable source label for relay prefixes."
        },
        "default_channel": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Default radio channel index for outbound messages."
        },
        "message_delay_seconds": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "description": "Minimum delay between outbound messages (pacing)."
        },
        "metadata_embedding": {
          "type": "boolean",
          "default": true,
          "description": "Whether to embed MEDRE metadata envelopes in LXMF fields."
        },
        "identity_path": {
          "type": ["string", "null"],
          "default": null,
          "description": "Path to a Reticulum identity file."
        },
        "storage_path": {
          "type": ["string", "null"],
          "default": null,
          "description": "Path for LXMF LXMRouter persistent storage. Required for 'reticulum' mode."
        },
        "reticulum_config_dir": {
          "type": ["string", "null"],
          "default": null,
          "description": "Path to an isolated Reticulum configuration directory for this adapter instance. When set, the adapter starts its own Reticulum with that configdir instead of the host default, enabling private per-instance radio setups."
        },
        "announce_interval_seconds": {
          "type": "number",
          "default": 600.0,
          "minimum": 0,
          "description": "Interval in seconds between periodic LXMF announces for mesh path discovery. 0 disables."
        },
        "lxmf_relay_prefix": {
          "type": "string",
          "default": "",
          "description": "Optional relay prefix template prepended to outbound LXMF message body text. Empty string means no prefix. Uses {placeholder} syntax resolved by the shared prefix formatter."
        }
      },
      "required": ["adapter_id"],
      "allOf": [
        {
          "if": {
            "properties": {
              "connection_type": { "const": "reticulum" },
              "default_delivery_method": { "const": "propagated" }
            },
            "required": ["connection_type", "default_delivery_method"]
          },
          "then": {
            "properties": {
              "outbound_propagation_node": {
                "type": "string",
                "pattern": "^[0-9A-Fa-f]{32}$"
              }
            },
            "required": ["outbound_propagation_node"]
          }
        }
      ],
      "additionalProperties": false
    }
  ]
}
