{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://medre.dev/schemas/delivery-result.schema.json",
  "title": "AdapterDeliveryResult",
  "description": "Immutable result returned by adapters after successful delivery. Adapters populate this with platform-native IDs obtained from the external system. Derived from dataclass(frozen=True).",
  "type": "object",
  "properties": {
    "native_message_id": {
      "type": ["string", "null"],
      "default": null,
      "description": "Platform-native message ID (e.g. a Matrix event_id). None when not yet available."
    },
    "native_channel_id": {
      "type": ["string", "null"],
      "default": null,
      "description": "Platform-native channel/room/conversation ID."
    },
    "native_thread_id": {
      "type": ["string", "null"],
      "default": null,
      "description": "Platform-native thread or parent message ID. Reserved — always None at runtime."
    },
    "native_relation_id": {
      "type": ["string", "null"],
      "default": null,
      "description": "Platform-native ID of the related entity. Reserved — always None at runtime."
    },
    "delivery_note": {
      "type": "string",
      "default": "",
      "description": "Human-readable context about the delivery. Informational only — not for control-flow."
    },
    "delivery_status": {
      "type": "string",
      "enum": ["sent", "enqueued"],
      "default": "sent",
      "description": "Adapter delivery fact: 'sent' (synchronous) or 'enqueued' (queue-based). Reports what the adapter did, not the final lifecycle state of the message. The pipeline maps this to receipt status; it is not lifecycle authority."
    },
    "metadata": {
      "type": "object",
      "propertyNames": {
        "not": {
          "enum": ["delivery_status", "adapter_status", "status", "state"]
        }
      },
      "additionalProperties": true,
      "default": {},
      "description": "Immutable, namespaced delivery metadata. Transport-specific data MUST live under metadata[<transport>] (e.g. metadata.matrix, metadata.lxmf). No top-level MEDRE-standard keys are permitted."
    }
  },
  "additionalProperties": false
}
