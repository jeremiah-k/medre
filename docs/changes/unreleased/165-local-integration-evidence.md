# Record deterministic local-integration evidence

- Mark the MeshCore real-SDK TCP and LXMF process-isolated local-integration
  gates as executed for the current Git tree.
- Distinguish deterministic real-SDK local integration from Docker validation
  and external live/hardware validation in the capability status vocabulary.
- Keep physical-node, external-network, and transport soak evidence explicitly
  open.
