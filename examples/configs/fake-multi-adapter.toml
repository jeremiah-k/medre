# MEDRE Example: Fake Multi-Adapter (Development / Testing)
#
# All four adapter types enabled with fake connection backends.
# No real hardware or Matrix homeserver required — everything is simulated.
#
# Use this for:
#   - Local development without hardware
#   - CI/CD pipeline testing
#   - Integration test baselines
#   - Rapid iteration on message routing logic

[runtime]
name = "fake-multi-dev"
shutdown_timeout_seconds = 10

[logging]
level = "DEBUG"
format = "text"

[storage]
backend = "filesystem"
path = "/tmp/medre-dev/store"

# --- Matrix (fake) ---
[adapters.matrix.fake_matrix]
enabled = true
homeserver = "https://fake.local"
user_id = "@dev-bot:fake.local"
access_token = "fake_token_for_dev"
room_allowlist = ["!fake-room:fake.local"]
encryption_enabled = false

# --- Meshtastic (fake) ---
[adapters.meshtastic.fake_meshtastic]
enabled = true
connection_type = "fake"
meshnet_name = "dev-radio"

# --- MeshCore (fake) ---
[adapters.meshcore.fake_meshcore]
enabled = true
connection_type = "fake"

# --- LXMF (fake) ---
[adapters.lxmf.fake_lxmf]
enabled = true
connection_type = "fake"
display_name = "MEDRE Dev LXMF"
