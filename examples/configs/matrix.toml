# MEDRE Example: Matrix Adapter
#
# Single Matrix adapter. Plaintext by default.
# For E2EE, change encryption_mode to "e2ee_required" and
# install the matrix-e2e extra: pip install "medre[matrix-e2e]"
# MEDRE derives device_id (via whoami) and store_path
# ({state}/adapters/{adapter_id}/matrix/store) internally — no operator
# configuration needed for either.
# Copy and adjust for your homeserver and credentials.

[runtime]
name = "matrix-bot"
shutdown_timeout_seconds = 10

[logging]
level = "INFO"
format = "text"

[storage]
backend = "sqlite"
path = "{state}/medre.sqlite"

# --- Matrix transport ---
[adapters.matrix.main]
enabled = true
homeserver = "https://matrix.example.com"
user_id = "@bot:example.com"
access_token = ""
room_allowlist = ["!room:example.com"]
encryption_mode = "plaintext"        # plaintext | e2ee_required | e2ee_optional
